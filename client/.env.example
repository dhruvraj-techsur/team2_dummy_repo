# Example environment variable for API URL
VITE_API_URL="https://your-api-url.com"

# Note: Prefix environment variables with 'VITE_' to expose them to client-side code in Vite.
# Avoid storing sensitive information in client-side environment variables for security reasons.