# Example environment variables for the client application
# Rename this file to `.env` and fill in the actual values before running the app

# Base URL of your backend API server
VITE_API_URL=https://your-api-url.com

# Optional: client app port (default depends on dev server)
VITE_PORT=3000