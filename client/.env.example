# Base URL for the backend API
VITE_API_BASE_URL=http://localhost:4000

# API endpoint for user login (relative to VITE_API_BASE_URL)
VITE_API_LOGIN_ENDPOINT=/api/login

# NOTE: Do NOT commit sensitive credentials or secrets in this file.
# Use environment-specific .env files or secret management solutions.