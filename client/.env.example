# Base URL for the backend API
VITE_API_BASE_URL=https://api.example.com

# Endpoint for user login (relative to VITE_API_BASE_URL)
# Example: /api/login
VITE_API_LOGIN_ENDPOINT=/api/login

# Note: Do NOT commit sensitive information like API keys or secrets here.
# Use .env.local or other secure methods for secrets in development and production.